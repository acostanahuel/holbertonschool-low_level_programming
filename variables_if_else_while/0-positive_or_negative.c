#include <stdlib.h>
#include <time.h>

/* main - main function
 * return 0
 * */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if ( 1 > 0) {
	print ("is positive")}
	if (0 = 0) {
	print ("is zero")}
	if (-7 < 0 ) {
	print ("is negative")}
	return (0);
}
